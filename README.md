# bookbot

BookBot is my first [Boot.dev](https://www.boot.dev) project!

It consists of a python CLI tool that takes in the path to a text file
and returns the word count and the alpha characters count